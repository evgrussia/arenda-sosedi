---
title: "Accessibility Guidelines"
created_by: "UX Agent"
created_at: "2025-02-05"
version: "1.0"
status: "approved"
---

# Accessibility Guidelines — Арендо-Соседи

## Введение

Документ определяет требования и рекомендации по доступности (a11y) для Telegram WebApp платформы Арендо-Соседи. Соблюдение этих требований обеспечивает возможность использования приложения людьми с различными ограничениями.

---

## 1. Стандарты и цели

### 1.1 Целевой уровень соответствия

| Стандарт | Уровень | Описание |
|----------|---------|----------|
| **WCAG 2.1** | AA | Основной стандарт веб-доступности |
| **Section 508** | Compliant | Для возможного выхода на US-рынок |
| **ГОСТ Р 52872** | Соответствие | Российский стандарт доступности |

### 1.2 Ключевые принципы (POUR)

```
P — Perceivable (Воспринимаемость)
    Информация должна быть представлена так,
    чтобы пользователи могли её воспринять

O — Operable (Управляемость)
    Интерфейс должен управляться
    различными способами ввода

U — Understandable (Понятность)
    Информация и управление должны
    быть понятными

R — Robust (Надёжность)
    Контент должен корректно
    интерпретироваться технологиями
```

---

## 2. Визуальная доступность

### 2.1 Цветовой контраст

#### Минимальные требования

| Элемент | Размер | Минимальный контраст | Рекомендуемый |
|---------|--------|---------------------|---------------|
| Основной текст | 16px+ | 4.5:1 | 7:1 |
| Крупный текст | 18px+ bold / 24px+ | 3:1 | 4.5:1 |
| UI-компоненты | — | 3:1 | 4.5:1 |
| Графика | — | 3:1 | 4.5:1 |

#### Проверка контраста

```yaml
Tools:
  - WebAIM Contrast Checker
  - Chrome DevTools Contrast
  - Figma Plugins (Stark, Contrast)

Primary Colors Check:
  - Primary (#4F46E5) on White: ✓ 7.2:1
  - Secondary (#10B981) on White: ✓ 4.6:1
  - Error (#EF4444) on White: ✓ 4.5:1
  - Text (#1F2937) on White: ✓ 12.6:1
```

### 2.2 Независимость от цвета

**Правило:** Цвет не должен быть единственным способом передачи информации.

```
✗ Неправильно:
┌─────────────────┐
│ 🟢 Активно      │  ← только цвет
│ 🔴 Отклонено    │
└─────────────────┘

✓ Правильно:
┌─────────────────┐
│ ✓ Активно       │  ← цвет + иконка + текст
│ ✗ Отклонено     │
└─────────────────┘
```

#### Применение в статусах

| Статус | Цвет | Иконка | Текст |
|--------|------|--------|-------|
| Успех | Green | ✓ | "Успешно" |
| Ошибка | Red | ✗ | "Ошибка" |
| Ожидание | Yellow | ⏳ | "В обработке" |
| Информация | Blue | ℹ | "Информация" |

### 2.3 Размер шрифтов

```yaml
Minimum font sizes:
  body_text: 16px (не меньше)
  secondary_text: 14px
  caption: 12px (только для некритичной информации)

Line height:
  minimum: 1.4
  recommended: 1.5

Letter spacing:
  body: normal
  headings: -0.01em (лёгкий tracking)
```

### 2.4 Масштабирование

```yaml
Support:
  - Pinch-to-zoom: enabled (не блокировать)
  - Text scaling: до 200% без потери функциональности
  - Dynamic Type (iOS): поддержка системных настроек

Testing:
  - Проверять UI при zoom 200%
  - Проверять при увеличенном системном шрифте
```

---

## 3. Навигация и фокус

### 3.1 Focus Management

#### Видимый фокус

```css
/* Базовый стиль фокуса */
:focus-visible {
  outline: 2px solid #4F46E5;
  outline-offset: 2px;
}

/* Для тёмного фона */
.dark :focus-visible {
  outline-color: #818CF8;
}

/* Никогда не делать */
:focus {
  outline: none; /* ✗ Запрещено */
}
```

#### Focus Order (Tab Order)

```
Порядок фокуса должен быть логичным и предсказуемым:

1. Header (back, title, actions)
2. Контент сверху вниз
3. Интерактивные элементы в порядке чтения
4. Bottom navigation / CTA
5. Modal → focus trap внутри модала

Правила:
- tabindex="0" — естественный порядок
- tabindex="-1" — программный фокус
- tabindex="1+" — НЕ ИСПОЛЬЗОВАТЬ
```

#### Focus Trap для модалов

```javascript
// При открытии модала
const modal = document.querySelector('.modal');
const focusableElements = modal.querySelectorAll(
  'button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])'
);
const firstElement = focusableElements[0];
const lastElement = focusableElements[focusableElements.length - 1];

// Trap focus inside modal
modal.addEventListener('keydown', (e) => {
  if (e.key === 'Tab') {
    if (e.shiftKey && document.activeElement === firstElement) {
      e.preventDefault();
      lastElement.focus();
    } else if (!e.shiftKey && document.activeElement === lastElement) {
      e.preventDefault();
      firstElement.focus();
    }
  }
});
```

### 3.2 Размер интерактивных элементов

```yaml
Touch targets:
  minimum: 44×44px (WCAG)
  recommended: 48×48px

Spacing between targets:
  minimum: 8px

Exceptions:
  - Inline links в тексте (но с достаточным padding)
```

### 3.3 Keyboard Navigation

| Действие | Клавиша |
|----------|---------|
| Следующий элемент | Tab |
| Предыдущий элемент | Shift + Tab |
| Активация | Enter / Space |
| Закрыть модал | Escape |
| Навигация по списку | Arrows |
| Выбор в dropdown | Enter |

---

## 4. Семантическая разметка

### 4.1 HTML Landmarks

```html
<header role="banner">
  <!-- Шапка приложения -->
</header>

<nav role="navigation" aria-label="Главное меню">
  <!-- Tab bar -->
</nav>

<main role="main">
  <!-- Основной контент -->
</main>

<aside role="complementary">
  <!-- Боковой контент (если есть) -->
</aside>

<footer role="contentinfo">
  <!-- Подвал -->
</footer>
```

### 4.2 Heading Hierarchy

```html
<!-- Правильная иерархия -->
<h1>Каталог</h1>
  <h2>Инструменты</h2>
    <h3>Дрель Bosch</h3>
  <h2>Спорт</h2>
    <h3>Велосипед</h3>

<!-- НЕ пропускать уровни -->
<h1>Каталог</h1>
<h3>Дрель</h3>  <!-- ✗ Пропущен h2 -->
```

### 4.3 ARIA Labels

```html
<!-- Иконки-кнопки -->
<button aria-label="Добавить в избранное">
  <HeartIcon />
</button>

<!-- Поиск -->
<input
  type="search"
  aria-label="Поиск товаров"
  placeholder="Найти вещь..."
/>

<!-- Динамический контент -->
<div
  aria-live="polite"
  aria-atomic="true"
>
  Найдено: 14 результатов
</div>

<!-- Состояния -->
<button
  aria-pressed="true"
  aria-label="Фильтр по цене активен"
>
  Цена ▼
</button>
```

### 4.4 Формы

```html
<!-- Связь label и input -->
<label for="item-name">Название товара</label>
<input
  id="item-name"
  type="text"
  aria-describedby="name-hint name-error"
  aria-invalid="false"
  required
/>
<span id="name-hint">От 5 до 100 символов</span>
<span id="name-error" role="alert"></span>

<!-- Группировка -->
<fieldset>
  <legend>Выберите категорию</legend>
  <input type="radio" id="cat-tools" name="category" />
  <label for="cat-tools">Инструменты</label>
  <!-- ... -->
</fieldset>
```

---

## 5. Screen Reader Support

### 5.1 Альтернативный текст для изображений

```html
<!-- Информативные изображения -->
<img
  src="drill.jpg"
  alt="Дрель Bosch GSB 13RE, синего цвета, вид сбоку"
/>

<!-- Декоративные изображения -->
<img src="decoration.svg" alt="" role="presentation" />

<!-- Сложные изображения -->
<figure>
  <img src="chart.png" alt="График роста аренд" />
  <figcaption>
    Рост количества аренд: январь 100, февраль 150, март 200
  </figcaption>
</figure>
```

### 5.2 Динамический контент

```html
<!-- Live regions -->
<div aria-live="polite">
  <!-- Некритичные обновления -->
  <span>Загружено ещё 10 товаров</span>
</div>

<div aria-live="assertive">
  <!-- Критичные уведомления -->
  <span>Ошибка: не удалось отправить сообщение</span>
</div>

<!-- Статус загрузки -->
<div role="status" aria-live="polite">
  <span class="sr-only">Загрузка...</span>
  <Spinner />
</div>
```

### 5.3 Скрытый контент

```css
/* Скрыть визуально, но оставить для SR */
.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}

/* Скрыть от SR */
[aria-hidden="true"] {
  /* Декоративные элементы */
}
```

### 5.4 Объявления для SR

| Событие | Объявление |
|---------|------------|
| Отправка формы | "Форма отправлена" |
| Ошибка валидации | "Ошибка: [описание]" |
| Успешное действие | "[Действие] выполнено" |
| Загрузка | "Загрузка, подождите" |
| Новое уведомление | "Новое уведомление: [текст]" |

---

## 6. Специфика Telegram WebApp

### 6.1 Системные настройки Telegram

```yaml
Наследование:
  - Тема (светлая/тёмная) — автоматически
  - Размер шрифта — поддерживать
  - Reduced motion — учитывать

Telegram.WebApp API:
  - colorScheme: 'light' | 'dark'
  - themeParams: цвета темы
```

### 6.2 Адаптация к системным настройкам

```javascript
// Тема
const isDark = Telegram.WebApp.colorScheme === 'dark';

// Reduced motion
const prefersReducedMotion = window.matchMedia(
  '(prefers-reduced-motion: reduce)'
).matches;

if (prefersReducedMotion) {
  // Отключить анимации
  document.documentElement.classList.add('reduce-motion');
}
```

```css
/* Отключение анимаций */
.reduce-motion * {
  animation-duration: 0.001ms !important;
  transition-duration: 0.001ms !important;
}
```

### 6.3 Haptic Feedback

```javascript
// Тактильная обратная связь (с проверкой)
function hapticFeedback(type = 'light') {
  if (Telegram.WebApp.HapticFeedback) {
    Telegram.WebApp.HapticFeedback.impactOccurred(type);
  }
}

// Использование
button.addEventListener('click', () => {
  hapticFeedback('medium');
  // действие
});
```

---

## 7. Тестирование доступности

### 7.1 Автоматическое тестирование

```yaml
Tools:
  - axe-core (browser extension)
  - Lighthouse Accessibility audit
  - eslint-plugin-jsx-a11y
  - jest-axe (unit tests)

CI Integration:
  - Axe в GitHub Actions
  - Threshold: 0 critical/serious violations
```

### 7.2 Ручное тестирование

#### Checklist

```markdown
## Keyboard Navigation
- [ ] Все интерактивные элементы достижимы через Tab
- [ ] Фокус виден на всех элементах
- [ ] Порядок фокуса логичен
- [ ] Модалы имеют focus trap
- [ ] Escape закрывает модалы

## Screen Reader (VoiceOver / TalkBack)
- [ ] Все изображения имеют alt
- [ ] Формы корректно размечены
- [ ] Кнопки имеют понятные названия
- [ ] Статусы озвучиваются
- [ ] Навигация понятна

## Visual
- [ ] Контраст соответствует AA
- [ ] Информация не только цветом
- [ ] UI работает при zoom 200%
- [ ] Шрифты читаемы

## Motion
- [ ] Анимации отключаются при prefers-reduced-motion
- [ ] Нет мигающего контента
```

### 7.3 Тестирование с пользователями

```yaml
Рекомендуется:
  - Тестирование со слабовидящими пользователями
  - Тестирование с пользователями screen reader
  - Тестирование с пользователями моторными ограничениями

Методы:
  - Удалённое юзабилити-тестирование
  - Партнёрство с организациями (ВОС и др.)
```

---

## 8. Рекомендации по компонентам

### 8.1 Кнопки

```html
<!-- Стандартная кнопка -->
<button
  type="button"
  class="btn btn-primary"
>
  Арендовать
</button>

<!-- Кнопка с иконкой -->
<button
  type="button"
  aria-label="Добавить в избранное"
  class="btn-icon"
>
  <HeartIcon aria-hidden="true" />
</button>

<!-- Кнопка загрузки -->
<button
  type="submit"
  disabled
  aria-busy="true"
>
  <Spinner aria-hidden="true" />
  <span>Отправка...</span>
</button>
```

### 8.2 Карточки товаров

```html
<article class="item-card">
  <a href="/item/123" aria-label="Дрель Bosch, 300 рублей в день">
    <img src="drill.jpg" alt="" /> <!-- alt пустой, т.к. есть aria-label -->
    <div class="item-info">
      <h3>Дрель Bosch</h3>
      <p class="price">300 ₽/день</p>
      <p class="rating">
        <span aria-label="Рейтинг 4.8 из 5">★★★★★ 4.8</span>
      </p>
    </div>
  </a>
  <button aria-label="Добавить Дрель Bosch в избранное">
    <HeartIcon aria-hidden="true" />
  </button>
</article>
```

### 8.3 Модальные окна

```html
<div
  role="dialog"
  aria-modal="true"
  aria-labelledby="modal-title"
  aria-describedby="modal-desc"
>
  <h2 id="modal-title">Фильтры</h2>
  <p id="modal-desc">Настройте параметры поиска</p>

  <!-- Контент модала -->

  <button aria-label="Закрыть фильтры">✕</button>
</div>
```

### 8.4 Табы

```html
<div role="tablist" aria-label="Мои аренды">
  <button
    role="tab"
    aria-selected="true"
    aria-controls="panel-renter"
    id="tab-renter"
  >
    Арендатор
  </button>
  <button
    role="tab"
    aria-selected="false"
    aria-controls="panel-owner"
    id="tab-owner"
  >
    Владелец
  </button>
</div>

<div
  role="tabpanel"
  id="panel-renter"
  aria-labelledby="tab-renter"
>
  <!-- Контент панели -->
</div>
```

---

## 9. Чеклист для разработчиков

### При разработке компонента

```markdown
## Accessibility Checklist

### Базовое
- [ ] Семантический HTML
- [ ] Корректный heading order
- [ ] Alt для изображений

### Интерактивность
- [ ] Keyboard accessible
- [ ] Visible focus state
- [ ] Touch target 44×44px minimum

### ARIA
- [ ] aria-label для icon buttons
- [ ] aria-live для динамического контента
- [ ] aria-invalid для форм с ошибками

### Визуальное
- [ ] Contrast ratio AA (4.5:1 text, 3:1 UI)
- [ ] Не только цветом передаётся информация
- [ ] Работает при zoom 200%

### Тестирование
- [ ] axe-core проверка
- [ ] Keyboard-only тест
- [ ] Screen reader тест
```

---

## Приложения

### A. ARIA Roles Reference

| Role | Использование |
|------|---------------|
| `button` | Кликабельные элементы |
| `link` | Навигационные ссылки |
| `dialog` | Модальные окна |
| `alert` | Важные уведомления |
| `status` | Статусные сообщения |
| `tab`, `tabpanel` | Табы |
| `listbox`, `option` | Выпадающие списки |

### B. Полезные ресурсы

- [WCAG 2.1 Guidelines](https://www.w3.org/WAI/WCAG21/quickref/)
- [WAI-ARIA Authoring Practices](https://www.w3.org/WAI/ARIA/apg/)
- [a11y Project Checklist](https://www.a11yproject.com/checklist/)
- [Inclusive Components](https://inclusive-components.design/)

### C. Контакты для вопросов по a11y

```yaml
Internal:
  - UX Team Lead
  - Frontend Tech Lead

External:
  - a11y консультанты (при необходимости)
```

---

*Документ создан: UX Agent | Дата: 2025-02-05*
